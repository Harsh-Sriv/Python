# ============================================================
# MODULE: study_app.utils.text_helpers
# ============================================================


def make_heading(text: str) -> str:
    """Return a simple heading string for the supplied text."""
    return f"--- {text} ---"
